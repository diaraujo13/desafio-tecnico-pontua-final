/**
 * Global Setup for E2E Tests
 *
 * This runs before all tests to ensure the app is built and ready.
 */

module.exports = async () => {
  // Detox will handle app launch automatically
  // This is a placeholder for any additional setup needed
  console.log('E2E Test Setup: Ready');
};
